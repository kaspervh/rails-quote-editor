<main class="container">
  <h1>Quote Editor</h1>
  <p>hotwire quote editor</p>

  <%= if user_signed_in? %>
    <%= link_to 'View Quotes', quotes_path, class: 'btn btn--dark' %>
  <%= else %>
    <%= link_to 'Login', new_user_session_path, class: 'btn btn--dark' %>
  <%= end %>
</main>