<div class="quote">
  <%= link_to quote.name, quote_path(quote)%>
  <div class="quote_actions">
    <%= button_to 'delete', quotes_path(quote), method: :delete, class: 'btn btn--light' %> 
    <%= link_to 'edit', edit_quotes_path(quote), class: 'btn btn--light' %> 
  </div>
</div>